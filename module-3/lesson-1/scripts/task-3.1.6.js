var preTipTotal=30;
var tipPercentage=0.1;
var totalBill=preTipTotal * (1+tipPercentage);

document.write ("Thankyou for your custom.  Your total bill including tip of £3 is £"+totalBill.toFixed(2));
